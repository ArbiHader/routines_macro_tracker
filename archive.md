# Archived streams

Streams and flashpoints the Curator has retired, each with a reason and date — never deleted (D-6).
Uses the same entry shape as `streams.md` plus an `archived: <date> — <reason>` line. Empty at seed.
